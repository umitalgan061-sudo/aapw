import { describe, expect, it } from 'vitest';
import { platformFingerprint, resolvePlatformPolicy, type PlatformSignals } from '../../src/3d/modern/platformRuntimeV4';
const signals=(overrides:Partial<PlatformSignals>={}):PlatformSignals=>({webgpu:true,webgl2:true,worker:true,offscreenCanvas:true,indexedDb:true,sharedArrayBuffer:true,hardwareConcurrency:8,deviceMemoryGb:8,saveData:false,reducedMotion:false,touchPoints:0,viewportWidth:1920,viewportHeight:1080,dpr:1,...overrides});
describe('platform runtime v4',()=>{it('selects webgpu on capable desktop',()=>expect(resolvePlatformPolicy(signals()).backend).toBe('webgpu'));it('fails closed to constrained policy',()=>{const policy=resolvePlatformPolicy(signals({saveData:true,hardwareConcurrency:2,deviceMemoryGb:2}));expect(policy.deviceClass).toBe('constrained');expect(policy.quality).toBe('minimal');expect(policy.textureBudgetBytes).toBe(32*1024*1024)});it('is deterministic',()=>{const a=signals({reducedMotion:true,webgpu:false});expect(platformFingerprint(a,resolvePlatformPolicy(a))).toBe(platformFingerprint(a,resolvePlatformPolicy(a)));});});
