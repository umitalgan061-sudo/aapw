name: Run 261 Editor FBX Hierarchy Exact Head

on:
  push:
    branches:
      - agent/run261-editor-fbx-hierarchy-inventory
    paths:
      - '.github/workflows/run261-editor-fbx-hierarchy-exact-head.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: run261-editor-fbx-hierarchy-exact-head-${{ github.ref }}
  cancel-in-progress: true

env:
  TZ: Europe/Istanbul

jobs:
  validate:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Exact-main and additive gates
        run: |
          set -euo pipefail
          git fetch origin main
          CURRENT_MAIN="$(git rev-parse origin/main)"
          test "$CURRENT_MAIN" = "$(git merge-base HEAD "$CURRENT_MAIN")"
          node scripts/checkAdditiveOnlyDiff.js "$CURRENT_MAIN" HEAD
          git diff --check
          grep -Fq '## Run 261 — World Editor FBX hierarchy inventory / child-selection preflight' 3D_GAME_PROGRESS.md
          grep -Fq 'run261,2026-08-11,editor-fbx-hierarchy-inventory' perf_log.csv
          grep -Fq 'Run261 editor FBX hierarchy inventory' STABLE_TAGS.md
      - name: Install Chromium proof dependency
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/pw"
          npm install --prefix "$RUNNER_TEMP/pw" --no-save --no-package-lock playwright@1.55.0
          "$RUNNER_TEMP/pw/node_modules/.bin/playwright" install --with-deps chromium
          echo "NODE_PATH=$RUNNER_TEMP/pw/node_modules" >> "$GITHUB_ENV"
      - name: Syntax and focused FBX hierarchy regression
        run: |
          set -euo pipefail
          node --check scripts/checkRun261EditorFbxHierarchyInventoryV2.js
          node --check src/3d/editor/EditorAssetManager.js
          node --check src/3d/editor/editorAssetLibrary.js
          node scripts/checkRun261EditorFbxHierarchyInventoryV2.js
      - name: PWA performance determinism governance and smoke
        run: |
          set -euo pipefail
          node scripts/checkServiceWorkerCache.js
          node scripts/checkPwaInstallability.js
          node scripts/checkMobilePerfBudget.js
          node scripts/checkSeededRandomPolicy.js
          node scripts/checkWorldReferenceMap.js
          node scripts/checkWorldReferenceWaterMask.js
          node scripts/checkWorldReferenceAlignment.js
          node scripts/checkWorldReferenceHydrologyExtent.js
          node scripts/terrainSeatSafetyCheck.js
          node scripts/roadNetworkSafetyCheck.js
          node scripts/analyzePerfTrend.js
          node scripts/checkTechnicalDebt.js
          node scripts/smokeTestGame3D.js
      - name: Final remote-main concurrency and additive gates
        run: |
          set -euo pipefail
          git fetch origin main
          CURRENT_MAIN="$(git rev-parse origin/main)"
          test "$CURRENT_MAIN" = "$(git merge-base HEAD "$CURRENT_MAIN")"
          node scripts/checkAdditiveOnlyDiff.js "$CURRENT_MAIN" HEAD
          git diff --check
      - name: Upload exact-head evidence
        uses: actions/upload-artifact@v4
        with:
          name: run261-editor-fbx-hierarchy-exact-head
          path: artifacts/run261-editor-fbx-hierarchy-inventory-v2
          if-no-files-found: error
