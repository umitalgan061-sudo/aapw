name: Live Map Shallow Water Visual

on:
  push:
    branches: ['fix/live-map-shallow-water-visual-exact-*']
  pull_request:
    branches: [main]
    paths:
      - 'src/3d/world/water.js'
      - 'src/3d/world/waterDepthField.js'
      - 'scripts/checkWaterVisualContract.js'
      - 'scripts/checkWaterSurfVisualContract.mjs'
      - 'scripts/checkWorldWaterCoverageP0.mjs'
      - 'scripts/checkWaterCoverageRasterQuality.mjs'
      - 'scripts/checkWaterCoverageSamplingBudget.mjs'
      - 'scripts/captureLiveWorldWaterTopdown.mjs'
      - '.github/workflows/live-map-shallow-water-visual.yml'
      - '.github/workflows/full-world-runtime-coverage.yml'
      - '.github/workflows/world-environment-acceptance.yml'
  workflow_dispatch:

permissions: { contents: read }
concurrency:
  group: live-map-shallow-water-visual-${{ github.ref }}
  cancel-in-progress: true

env: { TZ: Europe/Istanbul }

jobs:
  validate:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: false
          ref: '${{ github.event.pull_request.head.sha || github.sha }}'
      - uses: actions/setup-node@v4
        with: { node-version: 22 }
      - name: Exact-main bounded water scope and quota
        run: |
          set -euo pipefail
          git fetch origin main
          BASE_SHA="$(git rev-parse origin/main)"
          test "$(git merge-base HEAD origin/main)" = "$BASE_SHA"
          echo "BASE_SHA=$BASE_SHA" >> "$GITHUB_ENV"
          git diff --check "$BASE_SHA"...HEAD
          CHANGED="$(git diff --name-only "$BASE_SHA"...HEAD)"
          echo "$CHANGED"
          test "$(echo "$CHANGED" | grep -Ev '^(src/3d/world/water\.js|src/3d/world/waterDepthField\.js|scripts/checkWaterVisualContract\.js|scripts/checkWaterSurfVisualContract\.mjs|scripts/checkWorldWaterCoverageP0\.mjs|scripts/checkWaterCoverageRasterQuality\.mjs|scripts/checkWaterCoverageSamplingBudget\.mjs|scripts/captureLiveWorldWaterTopdown\.mjs|\.github/workflows/live-map-shallow-water-visual\.yml|\.github/workflows/full-world-runtime-coverage\.yml|\.github/workflows/world-environment-acceptance\.yml)$' | wc -l)" -eq 0
          echo "$CHANGED" | grep -Fx 'src/3d/world/water.js'
          echo "$CHANGED" | grep -Fx 'src/3d/world/waterDepthField.js'
          A="$(git diff --numstat "$BASE_SHA"...HEAD | awk '{s+=$1}END{print s+0}')"
          D="$(git diff --numstat "$BASE_SHA"...HEAD | awk '{s+=$2}END{print s+0}')"
          echo "shallow-water visual quota additions=$A deletions=$D total=$((A+D))"
          test $((A+D)) -le 1000
      - name: Install water contract runtime dependency
        run: |
          set -euo pipefail
          npm install --no-save --no-package-lock --ignore-scripts three@0.180.0
      - name: Static shader and coverage contract
        run: |
          set -euo pipefail
          node --check src/3d/world/water.js
          node --check src/3d/world/waterDepthField.js
          node --check scripts/checkWaterVisualContract.js
          node --check scripts/checkWaterSurfVisualContract.mjs
          node --check scripts/checkWorldWaterCoverageP0.mjs
          node --check scripts/checkWaterCoverageRasterQuality.mjs
          node --check scripts/checkWaterCoverageSamplingBudget.mjs
          node --check scripts/captureLiveWorldWaterTopdown.mjs
          node scripts/checkWorldWaterCoverageP0.mjs
          node scripts/checkWaterCoverageRasterQuality.mjs
          node scripts/checkWaterCoverageSamplingBudget.mjs
          node scripts/checkWaterSurfVisualContract.mjs
      - name: Hydrate scoped smoke-test LFS assets
        run: |
          set -euo pipefail
          git lfs version
          git lfs pull --include="assets/models/settlements/castles/**,assets/models/characters/**,assets/animations/peasant_girl/**,assets/models/animals/**,assets/models/creatures/dragon/**" --exclude=""
      - name: Install Playwright Chromium
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/pw"
          npm install --prefix "$RUNNER_TEMP/pw" --no-save --no-package-lock playwright@1.55.0
          "$RUNNER_TEMP/pw/node_modules/.bin/playwright" install --with-deps chromium
          echo "NODE_PATH=$RUNNER_TEMP/pw/node_modules" >> "$GITHUB_ENV"
      - name: Real GPU water, full-world live proof, mobile and browser safety
        run: |
          set -euo pipefail
          export NODE_PATH="$RUNNER_TEMP/pw/node_modules"
          node scripts/checkWaterVisualContract.js
          node scripts/checkRun325WaterSwell.js
          node scripts/captureLiveWorldWaterTopdown.mjs
          node scripts/checkMobilePerfBudget.js
          node scripts/smokeTestGame3D.js
      - name: Final exact-main freshness
        run: |
          set -euo pipefail
          git fetch origin main
          test "$(git rev-parse origin/main)" = "$BASE_SHA"
          git diff --check "$BASE_SHA"...HEAD
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: live-map-shallow-water-visual-proof
          if-no-files-found: warn
          path: |
            artifacts/run325-water-swell/
            artifacts/live-map-shallow-water-visual/
