name: Settlement Trade History View Model

on:
  pull_request:
    paths:
      - 'src/3d/gameplay/settlementTradeHistory.js'
      - 'src/3d/gameplay/settlementTradeHistoryViewModel.js'
      - 'scripts/checkSettlementTradeHistory.mjs'
      - 'scripts/checkSettlementTradeHistoryViewModel.mjs'
      - '.github/workflows/settlement-trade-history-view-model.yml'

permissions:
  contents: read

jobs:
  contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Syntax check
        run: node --check src/3d/gameplay/settlementTradeHistoryViewModel.js && node --check scripts/checkSettlementTradeHistoryViewModel.mjs
      - name: Execute contract
        run: node scripts/checkSettlementTradeHistoryViewModel.mjs
