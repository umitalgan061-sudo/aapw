name: Kızıl Ufuk Player Animation Equipment Sync

on:
  pull_request:
    branches: [main]
    paths:
      - 'src/3d/gameplay/playerAnimationEquipmentSyncDirector.js'
      - 'scripts/checkPlayerAnimationEquipmentSyncDirector.mjs'
      - '.github/workflows/player-animation-equipment-sync.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  contract:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 1
          lfs: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Syntax
        run: |
          node --check src/3d/gameplay/playerAnimationEquipmentSyncDirector.js
          node --check scripts/checkPlayerAnimationEquipmentSyncDirector.mjs
      - name: Runtime/editor boundary
        run: |
          ! grep -n "EditorMaterialStudio" src/3d/gameplay/playerAnimationEquipmentSyncDirector.js
          ! grep -n "from ['\"]three['\"]" src/3d/gameplay/playerAnimationEquipmentSyncDirector.js
          test -f src/3d/materials/MaterialAssignmentCore.js
          test -f src/3d/world/WorldAssetPlacementPipeline.js
      - name: Deterministic regression twice
        run: |
          node scripts/checkPlayerAnimationEquipmentSyncDirector.mjs > /tmp/sync-a.json
          node scripts/checkPlayerAnimationEquipmentSyncDirector.mjs > /tmp/sync-b.json
          cmp /tmp/sync-a.json /tmp/sync-b.json
      - name: Diff budget
        run: |
          base='${{ github.event.pull_request.base.sha || github.event.before }}'
          head='${{ github.event.pull_request.head.sha || github.sha }}'
          git fetch --no-tags origin "$base" --depth=1
          changed=$(git diff --numstat "$base" "$head" | awk '{a+=$1; d+=$2} END {print a+d+0}')
          test "$changed" -le 3000
