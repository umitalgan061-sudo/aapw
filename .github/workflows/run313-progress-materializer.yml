name: Run 313 Progress Materializer

on:
  push:
    branches:
      - run313-run303-dispatch-safety
    paths:
      - '.run313-progress-materialize'

permissions:
  contents: write

concurrency:
  group: run313-progress-materializer-${{ github.ref }}
  cancel-in-progress: false

env:
  BASE_SHA: fd11b1024570cc3b725a4ad9d4ec8bf98f95f855
  RUN_BRANCH: run313-run303-dispatch-safety
  TZ: Europe/Istanbul

jobs:
  materialize:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Fail closed outside Run313 source branch
        shell: bash
        run: |
          set -euo pipefail
          test "$GITHUB_REF" = "refs/heads/$RUN_BRANCH"

      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.sha }}

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Exact-main additive preflight
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main
          test "$(git rev-parse origin/main)" = "$BASE_SHA"
          git merge-base --is-ancestor "$BASE_SHA" HEAD
          node scripts/checkAdditiveOnlyDiff.js "$BASE_SHA" HEAD
          git diff --check "$BASE_SHA" HEAD
          node --check scripts/materializeRun313Progress.js

      - name: Install Chromium for real perf sample
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/pw"
          npm install --prefix "$RUNNER_TEMP/pw" --no-save --no-package-lock playwright@1.55.0
          "$RUNNER_TEMP/pw/node_modules/.bin/playwright" install --with-deps chromium
          echo "NODE_PATH=$RUNNER_TEMP/pw/node_modules" >> "$GITHUB_ENV"

      - name: Append Run313 progress checkpoint and performance row
        shell: bash
        run: |
          set -euo pipefail
          node scripts/collectPerfSnapshot.js run313-run303-dispatch-safety
          node scripts/materializeRun313Progress.js
          git diff --check
          git add 3D_GAME_PROGRESS.md STABLE_TAGS.md perf_log.csv
          git diff --cached --check
          git diff --cached --numstat | awk '$2 != 0 { print "non-additive staged change: " $0; exit 1 }'
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'Run313: materialize governance progress checkpoint'

      - name: Final exact-main gate and publish materialized head
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main
          test "$(git rev-parse origin/main)" = "$BASE_SHA"
          node scripts/checkAdditiveOnlyDiff.js "$BASE_SHA" HEAD
          git diff --check "$BASE_SHA" HEAD
          git push origin "HEAD:$RUN_BRANCH"
          TAG='stable-2026-08-12-run313'
          git tag "$TAG" HEAD
          git push origin "refs/tags/$TAG" || echo "::notice::Stable tag push remains blocked by the known GOVERNANCE 8.11 environment constraint"
