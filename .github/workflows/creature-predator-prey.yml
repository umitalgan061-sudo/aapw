name: Creature Predator Prey Ecology

on:
  pull_request:
    branches: [main]
    paths:
      - 'src/3d/gameplay/livingWorldSpawner.js'
      - 'scripts/checkCreaturePredatorPrey.mjs'
      - 'scripts/checkCreatureThreatMemory.mjs'
      - 'scripts/game3dSmokeChecksCreatureThreatMemory.js'
      - '.github/workflows/creature-predator-prey.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  contract:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Exact-main bounded scope
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main
          BASE_SHA="$(git rev-parse origin/main)"
          test "$BASE_SHA" = "$(git merge-base HEAD "$BASE_SHA")"
          CHANGED="$(git diff --numstat "$BASE_SHA" HEAD | awk '{a+=$1; d+=$2} END {print a+d+0}')"
          test "$CHANGED" -le 1000
          git diff --check "$BASE_SHA" HEAD
      - name: Predator prey and existing fauna contracts
        run: |
          set -euo pipefail
          node --check src/3d/gameplay/livingWorldSpawner.js
          node --check scripts/checkCreaturePredatorPrey.mjs
          node scripts/checkCreaturePredatorPrey.mjs
          node scripts/checkCreatureThreatMemory.mjs
          node scripts/checkCreatureSimulationLod.mjs
          node scripts/checkCreatureHabitatPlacement.mjs
      - name: Determinism PWA and shared material regressions
        run: |
          set -euo pipefail
          node scripts/checkSeededRandomPolicy.js
          node scripts/checkServiceWorkerCache.js
          node scripts/checkSharedMaterialPlacementContract.js
      - name: Final exact-main freshness
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main
          test "$(git merge-base HEAD origin/main)" = "$(git rev-parse origin/main)"
