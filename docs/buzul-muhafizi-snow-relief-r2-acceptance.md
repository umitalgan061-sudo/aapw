R2 acceptance definition: the generated corpus must contain exactly 4,096 deterministic production-derived cases, and the validator must replay every case against the canonical render-only snow surface resolver. Windward crust, lee powder, continuity, material tone hints, digest identity, and malformed-input safety are all asserted without modifying terrain, hydrology, collision, placement, or height ownership.