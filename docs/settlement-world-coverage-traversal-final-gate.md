# Traversal Final Gate

The traversal turn is scoped to settlement/world traversal evidence.

The implementation is deterministic.

The implementation is read-only.

The implementation is bounded.

Lane planning is bounded to ten lanes.

Route alternatives are bounded.

Risk factors are bounded to ten inputs.

Signals are bounded to twelve outputs.

Primary signals are bounded to four outputs.

Replay frames are bounded to sixteen frames.

Milestones are bounded to ten markers.

Scenario coverage contains seventy cases.

Mobile keeps shared semantics.

Accessibility keeps shared semantics.

Null and numeric inputs fail safely.

Fingerprints remain stable.

Ownership guards remain explicit.

No renderer mutation is introduced.

No road mutation is introduced.

No terrain mutation is introduced.

No actor spawning is introduced.

No save mutation is introduced.

No quest mutation is introduced.

The exact-head workflow checks the focused diff.

The merge target is main.

The final diff must remain above 2700 lines.

The final diff must remain below 3000 lines.

Merge is allowed only after the PR is mergeable.

Merge is allowed only after final checks are reviewed.

End of gate.
