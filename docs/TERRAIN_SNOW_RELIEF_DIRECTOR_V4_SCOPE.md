# Exact-main v4 scope

Base: `511122b34abcb1d18dca70a376baca2f068767c1`

This branch is intentionally capped below the 3000-line automation budget while carrying a complete render-facing snow relief production slice. The changed paths are limited to snow-relief runtime, terrain environment context/continuity qualification, browser evidence and their dedicated workflows.

The production authority remains canonical terrain height, hydrology, collider and owner-map geography. The new director only emits finite bounded render/material weights and deterministic field digests. The browser probe imports the shipped ES module and rejects page/console errors.

Current compare is expected to remain 0 behind exact main and within `250 <= additions+deletions <= 3000`.
