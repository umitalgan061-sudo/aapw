# NextGen Performance Budgets

Low, balanced, high and ultra tiers keep entity, streaming, render and network ceilings explicit.

Budget controllers may reduce optional work, but must never bypass deterministic simulation, persistence integrity or input/security validation.
