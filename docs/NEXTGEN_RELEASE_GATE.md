# NextGen Release Gate

A release candidate is accepted only when the typed runtime builds, deterministic tests pass, payload limits remain enforced, save checksums validate, and runtime budgets stay bounded on every supported device tier.

Performance regressions must be measured rather than hidden behind disabled checks. Legacy adapters may remain only at explicit ownership boundaries and must not become duplicate sources of gameplay truth.
